================================================================================
COMMUNICATION: BACKEND ARCHITECTURE & DEVELOPMENT LEDGER
================================================================================
Project: Bharat Railway Booking System
Branch: feature/backend-developer-hitanshu
Objective: Phase 1 Backend API (Login, Registration, Forgot Password)
Protocol: Enterprise-level technical peer-to-peer exchange.
================================================================================

[COMMUNICATION RULEBOOK & PROTOCOL]

Role Delineation:
- PM (Koushal Jha): I define the business logic, architectural constraints, API contracts, and audit the final code.
- BE (Hitanshu Dhakrey): Lead Backend Developer. Responsible for Java 21 / Spring Boot 3 implementation, Domain-Driven Design (DDD), security, and core application logic.
- DB/Infra (Chandra Shekhar Bansal): Infrastructure support. Responsible for assisting BE with PostgreSQL bindings, Kafka configurations, and Docker network troubleshooting.
- Scribe: The AI acts solely as a technical scribe to format the ledger based on PM instructions. 

Backend Coding Standards:
1. Framework: Java 21 with Spring Boot 3.x utilizing Virtual Threads (Project Loom).
2. Architecture: Domain-Driven Design (DDD). Strict separation between `identity` and `auth` domains.
3. Database Access: Spring Data JPA/Hibernate talking directly to `identity_schema` and `auth_schema`.
4. Quality: No dummy data. Strict input validation (Jakarta Validation). Standardized global exception handling mapping to standardized JSON responses.

Flow of Communication:
- The ledger is updated only upon PM initiation.
- Every exchange follows: 
  PM: [Specific Directive / API Contract / Logic Requirement]
  BE: [Technical Implementation Plan / Code / Acknowledgment]
- No "Hi/Hello" or AI filler. The dialogue must be purely technical.

================================================================================
ENTRY #01 - Backend Initialization & Rule Acknowledgement
================================================================================
Date: 2026-06-19
Subject: Phase 1 Onboarding & Protocol Confirmation

[INITIATOR: Project Manager - Koushal Jha]
Hitanshu, welcome to the backend development track. Chandra has successfully provisioned the infrastructure (PostgreSQL 16, PgBouncer, Kafka, Valkey, MinIO, Prometheus, Loki) and generated the DDL for the `identity_schema` and `auth_schema`.

Your scope for Phase 1 is strictly the Authentication and Identity domains:
1. Registration API (creating user records with strict validation).
2. Login API (authenticating credentials, generating JWTs, logging sessions to DB).
3. Forgot Password Flow (generating secure reset tokens, verifying channels).

Before we draft the `pom.xml` or define the directory structure, read the Communication Rulebook and Backend Coding Standards above. 

Directive for BE: 
Reply with your technical acknowledgment of these rules. Detail your understanding of how you plan to implement the Domain-Driven Design for this Phase 1 scope, and confirm your understanding of the Java 21/Spring Boot 3 requirement. Do not write any code yet. 

Status: AWAITING BE ACKNOWLEDGMENT

================================================================================
ENTRY #02 - Phase 1 Business Rules & API Contracts
Date: 2026-06-19
Subject: Architecture Correction and Business Logic Definitions
================================================================================

[PM - Koushal Jha]
Hitanshu, your DDD plan is approved with one critical infrastructure correction: Do not configure Dual DataSources. 

We are operating within a single PostgreSQL database (`bharat_railway_core`) using logical schemas. Managing distributed transactions across dual DataSources adds unnecessary connection overhead and transaction management complexity. Use a single HikariCP DataSource. Map the domains directly at the entity level using `@Table(schema = "identity_schema", name = "users")` and `@Table(schema = "auth_schema", name = "user_sessions")`.

Here are the strict business rules and API contracts for your implementation:

1. Registration API:
- Password Policy: Minimum 8 characters, at least 1 uppercase letter, 1 lowercase letter, 1 number, and 1 special character. Enforce this via a custom Jakarta `@Constraint` validator.
- Flow: Return HTTP 201 Created upon success with an empty body. Do not auto-generate a session. The client must explicitly call the login endpoint post-registration.

2. Login API:
- JWT Specifications: TTL is strictly 60 minutes. Payload claims must include `user_id`, `username`, and `status`.
- Audit: Every successful login must generate a UUID for the session, store it in `auth_schema.user_sessions`, and log the event in `auth_schema.login_history`. Account lockout logic (based on failed_login_attempts) must be enforced.

3. Forgot Password Flow:
- Flow: Do not mock the notification. You must implement a Spring Kafka Producer. Upon generating the secure reset token, push an event payload (userId, email, resetToken, channel) to a Kafka topic named `notification.events`. The future Notification service will consume this.

Directive for BE:
Based on these finalized rules, provide the exact core `pom.xml` dependencies required for this Java 21/Spring Boot 3 stack. Following that, map out the absolute package directory structure for the `bharat-railway-backend` repository.

----------------------------------------------------------------------------